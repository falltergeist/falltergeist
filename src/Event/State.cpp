#include "../Event/State.h"

namespace Falltergeist
{
    namespace Event
    {
        State::State(const std::string& name) : Event(name)
        {
        }

        State::~State()
        {
        }
    }
}
